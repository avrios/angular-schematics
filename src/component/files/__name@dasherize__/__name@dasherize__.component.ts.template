import { IComponentOptions, IController } from 'angular';

import './<%= dasherize(name) %>.scss';

class <%= classify(name) %>Controller implements IController {
    constructor() {
        'ngInject';
    }

    public $onInit(): void {
        //
    }
}

export const <%= classify(name) %>Component: IComponentOptions = {
    bindings: {
        //
    },
    controller: <%= classify(name) %>Controller,
    controllerAs: 'vm',
    templateUrl: './<%= dasherize(name) %>.html'
};
